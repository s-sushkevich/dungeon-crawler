import React from 'react';

import Map from '../containers/Map';

const Game = (props) => {
    return (
        <div className="game">
            <Map/>
        </div>
    )
};

export default Game;